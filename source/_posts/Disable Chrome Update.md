---
title: mac禁用chrome更新
---
### 禁止更新

在终端输入:

`cd ~/Library/Google`
`sudo chown root:wheel GoogleSoftwareUpdate`

### 恢复更新

在终端输入:

`cd ~/Library/Google`
`sudo chown root:staff GoogleSoftwareUpdate`
